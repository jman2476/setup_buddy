// this will eventually be a list of tests to run,
// but for now it will be a log of scenarious and outcomes
// will list which test border to use (triangle, square, diamond, pentagon);
// where the table starts; where the table moves to;
// and whether each line return true or false

const triangleCoords = [[100,100],[100,500],[446,300]]
const squareCoords = [[100,100],[500,100],[500,500],[100,500]]
const diamondCoords = [[400,100],[500,400],[200,500],[100,200]]
const pentagonCoords = [[300,500],[490,361],[417,138],[182,138],[109,361]]

const triangleTest = {
   0:{
      table_start: [,],
      table_end: [,],
      returns:[,,]
   },
}

const squareTest = {
   0:{
      table_start: [,],
      table_end: [,],
      returns:[,,,]
   },
}

const diamondTest = {
   0:{
      table_start: [,],
      table_end: [,],
      returns:[,,,]
   },
}

const pentagonTest = {
   0:{
      table_start: [,],
      table_end: [,],
      returns:[,,,,]
   },
}